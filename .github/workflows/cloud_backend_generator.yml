name: AO Cloud Backend Generator

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]

jobs:
  build-backend:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run Cloud Backend Generator
        run: bash cloud_backend_generator.sh
