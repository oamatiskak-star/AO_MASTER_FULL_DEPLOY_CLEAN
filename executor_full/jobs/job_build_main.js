module.exports = {
name: "job_build_main",

async run({ log }) {
log("BUILD MAIN job gestart")

// Hier kan je straks echte build triggers plaatsen
log("Main branch build flow uitgevoerd")

return true


}
}
